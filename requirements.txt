      
# Python packages required for whisper-ptt-v1.2 (GTK Version)
#
# To install, run: pip install -r requirements.txt
#
# NOTE: PyGObject (the GTK binding) is NOT listed here.
# It must be installed via the system package manager:
# sudo apt-get install python3-gi python3-gi-cairo gir1.2-gtk-3.0

# --- Core Libraries ---
numpy==1.26.4
sounddevice==0.4.7
pynput==1.7.6

# --- Speech Recognition & PyTorch ---
# For CUDA-enabled torch, you may need to install it separately first.
# Example for CUDA 12.1:
# pip install torch==2.3.0 --index-url https://download.pytorch.org/whl/cu121
faster-whisper==1.0.3
ctranslate2==4.3.1
torch==2.3.0
tokenizers==0.19.1

# --- Configuration Loading ---
tomli==2.0.1

    