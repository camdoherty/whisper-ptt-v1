      
# Python packages required for whisper-ptt-v1
#
# To install, run:
# pip install -r requirements.txt
#
# Note on PyTorch: As of version 2.0+, installing 'torch' via pip will
# automatically detect and install the appropriate CUDA 12 runtime libraries
# as separate 'nvidia-*' packages. This is the new standard method.

# Core libraries
numpy==1.26.4
sounddevice==0.4.7
pynput==1.7.6

# Speech recognition and its dependencies
faster-whisper==1.0.3
ctranslate2==4.3.1
# The following line will automatically pull in CUDA 12 support
torch==2.3.0

# Optional but recommended for tokenization performance
tokenizers==0.19.1

    